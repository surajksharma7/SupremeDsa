#include<iostream>
using namespace std;

int main(){

    // condition ? expression if true : else false ;
    // int age = 18;
    // (age<=18)? cout<<"Age is not greater than equal to 18":cout<<"age is less than 18";

     int x = 10;
     int y = 20;

     int result = (x>y)?x:y;
     cout<<"the larger value is "<<result <<endl;   
    return 0;

}